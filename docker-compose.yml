services:
  web:
    container_name: web
    build:
      context: ./web
      dockerfile: dev.Dockerfile

    env_file:
      - ./web/.env
    volumes:
      - ./web:/app
    restart: always
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true
