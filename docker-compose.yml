services:
  web:
    container_name: web
    build:
      context: ./web
      dockerfile: dev.Dockerfile

    env_file:
      - ./web/.env
    volumes:
      - ./web:/app
    restart: always
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true

  swagger:
    image: swaggerapi/swagger-ui
    container_name: swagger
    ports:
      - 8080:8080
    volumes:
      - ./spec/dist/openapi.yaml:/spec/dist/openapi.yaml:ro
    environment:
      SWAGGER_JSON: /spec/dist/openapi.yaml
